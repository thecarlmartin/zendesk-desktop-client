{
  "name": "Zendesk_Martin",
  "version": "0.0.1",
  "main": "./main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager ./ DesktopSupport --all --out ~/Desktop/Zendesk --version=0.35.1 --overwrite --icon=./app/img/app-icon.icns"
  },
  "devDependencies": {
    "electron-packager": "^5.1.1",
    "electron-prebuilt": "^0.35.1",
    "jquery-placeholder": "^2.1.1"
  },
  "dependencies": {
    "nconf": "^0.7.2"
  }
}
